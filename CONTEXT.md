# MyWiki Knowledge Map Context

这个上下文定义 MyWiki 知识地图的领域语言。知识地图服务于知识导航与发现，帮助用户从一个已知页面找到相关概念、相邻主题、孤立知识和潜在关联。

## Language

**知识地图**：面向知识导航与发现的页面关系视图。它帮助用户探索已维护知识之间的关系，不替代原始来源、Wiki 页面或引用体系。

_Avoid_: 纯 3D 向量图、知识库副本

**知识导航**：从一个已知知识页面出发，发现相关概念、相邻主题、孤立知识和潜在关联的活动。

_Avoid_: 全自动知识分类、向量检索系统

**知识节点**：一个已经进入 `wiki/` 知识层并具有自身页面的 source、concept 或 synthesis。原始材料、外部来源实例和临时文件不是第一版地图中的知识节点。

_Avoid_: 原始文件节点、文本分块节点、标签节点

**显式关系**：由 Wiki 页面中的 `[[wikilinks]]` 或 `sources` 依赖直接表达的页面关系。它代表已有的知识证据。

_Avoid_: 模型推测关系、仅因标签相同形成的关系

**推断关系**：由语义相似度或其他派生分析得到的页面关联。它用于探索提示，不等同于已确认的知识事实。

_Avoid_: 事实关系、权威关系

**主题标签**：用于对知识节点进行分类、筛选和聚类的属性，不单独构成地图中的关系线。

_Avoid_: 标签节点、隐含关系

**默认导航视图**：以清晰定位节点、阅读关系和筛选主题为优先的 2D 地图视图。

_Avoid_: 唯一视图、事实坐标系

**探索视图**：用于观察整体空间分布和发现远近关系的可选 3D 地图视图。它是默认导航视图的派生展示，不改变节点和关系的含义。

_Avoid_: 3D 真相空间、独立知识模型

**节点详情**：用户选中知识节点后看到的上下文面板，展示页面摘要、类型、标签、来源、显式关系、推断关系和更新时间，并提供打开原始 Wiki 页面的动作。

_Avoid_: 独立副本、编辑器替代品

**地图投影**：从 `wiki/` 知识层派生出来、可以重新生成的导航视图。它不拥有知识事实，也不改变页面、来源或引用。

_Avoid_: 第二知识库、同步副本、事实来源

**地图快照**：一次明确生成得到的、带版本和 manifest 的不可变派生地图数据。它是图谱查询的输入，不是新的事实来源。

_Avoid_: 可直接编辑的图数据库、实时主存储、第二知识层

**图谱查询**：针对一个地图快照请求有限范围图谱结果的只读查询，包含起点、关系层、展开范围、关系预算和筛选条件。

_Avoid_: 修改地图、隐式全图扫描、没有边界的全连接请求

**子图结果**：一次图谱查询返回的有边界节点和关系集合，同时说明被省略的内容、继续展开信息和对应的地图快照版本。

_Avoid_: 把局部结果伪装成完整地图、静默截断、无版本的临时图

**页面链接图**：由 Wiki 页面中的 `[[wikilinks]]` 和 `sources` 形成的显式关系层。它表达页面已经声明的连接。

_Avoid_: 语义猜测、实体自动抽取结果、与语义关系混为一层

**语义探索图**：由 embedding 或其他派生分析得到的推断关系层，用于发现潜在相邻页面。它不等同于页面链接图或事实关系。

_Avoid_: 权威知识图谱、默认实体图、未经说明的事实边

**图谱来源证据（provenance）**：用于回溯节点、关系和一次子图结果如何产生的结构化信息，包括来源页面、字段或关系声明、输入 hash、生成配置、地图快照和查询条件。

_Avoid_: 只有一个不可解释分数、脱离来源的说明文字、可替代事实源

**显式刷新**：由用户或一次明确的 ingest 流程主动触发地图投影重新生成的动作。

_Avoid_: 后台实时同步、隐式扫描

**只读地图**：只能选择、筛选、查看和跳转知识节点，不能直接修改 Wiki 页面、来源或关系的地图投影。

_Avoid_: 地图编辑器、第二写入入口

**核心导航场景**：用户从一个已知 Wiki 页面出发，在地图中发现一个原本不知道但相关的页面，查看关联依据，并在 2～3 次交互内打开该页面。

_Avoid_: 只验证视觉效果、只验证节点数量

**关系依据**：用于解释一条推断关系为何出现的可见说明，例如语义相似、共享主题标签、共享来源或相近概念。关系依据必须让用户知道该关系是推断结果，而不是页面已经明确声明的链接。

_Avoid_: 不透明分数、模型自评为事实

**关系预算**：地图为每个知识节点保留的可见关系数量和优先级。显式关系优先完整保留，推断关系默认限量展示，以维持可导航性。

_Avoid_: 全连接图、无阈值关系

**位置稳定性**：当地图输入、embedding 模型、降维算法和参数不变时，同一知识节点应保持可重复的空间位置。数据或模型变化导致的重新布局属于新的地图版本。

_Avoid_: 每次刷新随机重排、不可解释的坐标漂移

**主题子图**：从完整 Wiki 中按维护范围和主题边界选出的可独立探索的一组知识节点。第一版主题子图聚焦 AI 工程资料，不包含操作页或未完成来源摘要。

_Avoid_: 全 Wiki 首发、按目录机械切片

**导航验收场景**：用于验证地图是否真正帮助知识发现的固定操作路径，包括从已知页面发现未知相关页面、查看关系依据并打开目标 Wiki 页面，同时检查关系完整性、位置稳定性和无写入副作用。

_Avoid_: 只看截图、只统计节点数量、先追求模型分数

**语义空间**：由知识节点维护文本的 embedding 表示形成的相似性空间，用于生成地图坐标和候选推断关系。它描述内容相近程度，不直接声明知识事实。

_Avoid_: 事实空间、可解释的三条坐标轴

**关系叠加层**：绘制在语义空间上的显式 Wiki 关系，包括 `[[wikilinks]]` 和 `sources` 依赖。它表达页面明确声明的连接，不负责重新计算语义位置。

_Avoid_: 用链接替代 embedding、把所有线当成同一种关系

**embedding 输入**：经过维护并纳入第一版主题子图的 Wiki 页面内容，包括标题、摘要、关键点、标签和正文。原始来源不是 embedding 输入。

_Avoid_: 全量原始材料、未经审核的外部内容

**embedding 后端**：为 Wiki 页面生成语义向量的可替换计算实现。具体后端、模型、版本和参数属于地图生成配置，必须记录但不成为 Wiki 的领域事实。

_Avoid_: 固定供应商、不可替换的知识来源

**派生地图数据**：由 Wiki 页面和地图生成配置计算出的 embedding、坐标、推断关系和地图 manifest。它可以删除并重新生成，不能直接编辑，也不能取代 Wiki 页面、来源或引用。

_Avoid_: 第二事实源、手工维护的坐标库

**本地地图查看器**：独立于 Hermes Studio 的本地只读界面，加载派生地图数据并把节点详情链接回 MyWiki 页面。它不拥有 Wiki 数据，也不要求 Hermes Studio 作为运行时依赖。

_Avoid_: Hermes Studio 插件、第二个知识维护客户端

**静态知识导航**：基于某一次显式生成的地图版本进行页面探索，节点位置和关系不会因查看行为自动变化；节点更新时间可以作为详情信息展示。

_Avoid_: 时间回放、实时学习轨迹

**未定位节点**：已纳入主题子图但没有成功获得语义坐标的知识节点。它仍然可见并保留可用的显式关系，同时在地图和生成报告中说明缺失或失败原因。

_Avoid_: 静默丢弃、默认坐标、伪造 embedding

**远程 embedding**：使用外部服务生成 Wiki 页面向量的地图生成方式。它必须由用户显式启用，只能处理允许的 Wiki 内容，并在地图 manifest 中记录服务和模型信息。

_Avoid_: 默认外发、隐式切换供应商、上传原始来源

**视觉编码**：地图对知识节点语义的稳定表达：颜色表示主题，形状表示页面类型，第一版不使用节点大小表达重要性；选中状态通过高亮表达。

_Avoid_: 颜色同时表达主题和权威性、大小伪装成重要性排名

**聚焦模式**：筛选主题或页面类型时保留完整地图结构，只降低不匹配节点和关系的视觉权重；用户可以额外切换到严格隐藏模式。

_Avoid_: 默认删除上下文、把隐藏节点当成不存在

**非空间入口**：与地图平等的搜索或节点列表入口，用于直接定位知识节点，并与地图选择、详情面板和页面跳转保持同步。

_Avoid_: 只支持空间点击、把 3D 当作唯一入口

**主题聚类**：在节点数量增长后，将语义相近的知识节点先收敛为可展开的主题单元，以保持地图的可导航性。搜索仍可直接定位到单个节点。

_Avoid_: 无限增加关系线、只靠缩小画布解决规模
